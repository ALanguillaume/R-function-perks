
#' Time to write a function yourself ! Let's write a function called `raise_to_power()`
#' that takes an argument `x` and another argument `n`, computes `x ^ n`
#' (`x` to the power `n`) and return the result !
#' Fill in the blanks ('_____')

'______' <- '_______'('___', n = 2) {
  result <- x ^ n
  '______'(result)
}

# This should return 4
raise_to_power(2)
